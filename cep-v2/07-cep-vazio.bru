meta {
  name: 07 - CEP vazio
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/cep/v2/
  body: none
  auth: none
}

tests {
  test("retorna erro para CEP vazio", function() {
    expect(res.getStatus()).to.be.gte(400);
  });
}
