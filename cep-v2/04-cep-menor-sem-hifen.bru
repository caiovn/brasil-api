meta {
  name: 04 - CEP invalido menor sem hifen
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/cep/v2/1234567
  body: none
  auth: none
}

tests {
  test("retorna erro para CEP com menos de 8 digitos", function() {
    expect(res.getStatus()).to.be.gte(400);
  });
}
