meta {
  name: 05 - ISBN com menos digitos
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/isbn/v1/97885457028?providers=cbl
  body: none
  auth: none
}

tests {
  test("retorna erro para ISBN com menos digitos", function() {
    expect(res.getStatus()).to.be.gte(400);
  });
}
