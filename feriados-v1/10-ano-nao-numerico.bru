meta {
  name: 10 - Ano nao numerico
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/feriados/v1/abcd
  body: none
  auth: none
}

tests {
  test("retorna erro para ano nao numerico", function() {
    expect(res.getStatus()).to.be.gte(400);
  });
}
