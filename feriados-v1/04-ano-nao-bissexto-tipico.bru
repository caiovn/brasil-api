meta {
  name: 04 - Ano nao bissexto tipico
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/feriados/v1/2023
  body: none
  auth: none
}

tests {
  test("retorna lista de feriados para ano nao bissexto", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an("array");
  });
}
