meta {
  name: 07 - Ano limite superior
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/feriados/v1/2199
  body: none
  auth: none
}

tests {
  test("retorna lista de feriados para o limite superior", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an("array");
  });
}
