meta {
  name: 03 - Ano bissexto tipico
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/feriados/v1/2000
  body: none
  auth: none
}

tests {
  test("retorna lista de feriados para ano 2000", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an("array");
  });
}
