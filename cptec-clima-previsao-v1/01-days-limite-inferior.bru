meta {
  name: 01 - Days no limite inferior
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/cptec/v1/clima/previsao/{{validCityCode}}/1
  body: none
  auth: none
}

tests {
  test("retorna sucesso para days igual a 1", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an("array");
  });
}
